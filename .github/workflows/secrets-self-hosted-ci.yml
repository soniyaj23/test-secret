name: MATLAB Build

on:
  # push:
  #   branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted, matlab-r2026a]
    # env:
    #     MW_START_MSH_IN_BATCH: 1
    steps:
      - uses: actions/checkout@v6

      - name: 1st Run of testSecretAPIs script
        uses: matlab-actions/run-command@v3
        # with:
        #   command: listSecrets
        env:
           DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        with:
          command: disp(getenv('DB_PASSWORD'))
          
      # - name: 2nd Run of testSecretAPIs script
      #   uses: matlab-actions/run-command@v3
      #   with:
      #     command: testSecretAPIs;
